/* Eva Colom
Ejercicio 9 */

function mostrar()
{
	
	let numero;
	let numeromaximo = 10;
	let numerominimo = 1;

         numero = Math.round(Math.random() * (numeromaximo - numerominimo) + minimo);

         alert(numero);
}